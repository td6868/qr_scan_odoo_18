<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Default Dashboard: Stock Picking -->
        <record id="dashboard_stock_picking" model="dashboard.item">
            <field name="name">Dashboard Vận Chuyển</field>
            <field name="description">Theo dõi đơn hàng vận chuyển và quét QR code</field>
            <field name="url">/dashboard/stock_picking</field>
            <field name="icon">fa-truck</field>
            <field name="color">primary</field>
            <field name="sequence">10</field>
            <field name="open_new_tab" eval="True"/>
        </record>

        <!-- Example Dashboard 2 (có thể bỏ comment sau) -->
        <!-- <record id="dashboard_sales" model="dashboard.item">
            <field name="name">Dashboard Bán Hàng</field>
            <field name="description">Thống kê doanh số và đơn hàng</field>
            <field name="url">/dashboard/sales</field>
            <field name="icon">fa-shopping-cart</field>
            <field name="color">success</field>
            <field name="sequence">20</field>
            <field name="open_new_tab" eval="True"/>
        </record> -->

        <!-- Example Dashboard 3 (có thể bỏ comment sau) -->
        <!-- <record id="dashboard_inventory" model="dashboard.item">
            <field name="name">Dashboard Kho Hàng</field>
            <field name="description">Quản lý tồn kho và xuất nhập kho</field>
            <field name="url">/dashboard/inventory</field>
            <field name="icon">fa-boxes</field>
            <field name="color">warning</field>
            <field name="sequence">30</field>
            <field name="open_new_tab" eval="True"/>
        </record> -->
    </data>
</odoo>
